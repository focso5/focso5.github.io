I"�<p>JavaScript란?
프로그래밍적으로 웹브라우저를 제어하기위한 언어.</p>
<ul>
  <li>탈웹브라우저
  =&gt; 웹서버를 동작하기위한 도구로 사용(서버사이드스크립트)
  node.js(웹서버에서 사용하는 자바스크립트), php, java, python …</li>
</ul>

<p>언어란?</p>
<ul>
  <li>의사소통을 위한 약속</li>
</ul>

<p>환경이란(웹브라우저, node.js, SpreadSheet)?</p>
<ul>
  <li>다양한 분야에서 자바스크립트가 사용되고있음.</li>
  <li>언어를 사용하는 대상.</li>
  <li>자바스크립트를 사용할때 사용환경에 맞는 문법을 사용해야 한다.</li>
</ul>

<p>수학 관련 가테고리 
Math.pow() : 제곱
    =&gt; Math.pow(3,2)  // 3의 2승 = 9</p>

<p>Math.round() : 반올림
   =&gt; Math.round(10.6)  // 11</p>

<p>Math.ceil() : 소수점 버리고 올림
   =&gt; Math.ceil(10.2)  // 11</p>

<p>Math.floor() : 소수점 버림.
   =&gt; Math.floor(10.2)   // 10</p>

<p>Math.sqrt() : 제곱
   =&gt; Math.sqrt(9)  // 9의 제곱근은 3</p>

<p>Math.random() : 랜덤수
    =&gt; 100 * Math.random() // 100보다 작은 임의의 숫자를 출력</p>

<p>문자
    - escape 
        alert(‘egoing\ ‘s coding everybody’);
        =&gt; 홑따옴표(‘)로 묶었는데 문자열에 홑따옴표가 또 들어갔을 경우 역슬래시()를 사용한다.
    - typeof
        typeof 1   // “number”
        typeof “1”  // “string”
        =&gt; 데이터 타입이 무엇인지 궁금할때</p>
:ET